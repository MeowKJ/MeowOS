# MeowOS 0.2.0 内部质量评分

本报告区分静态证据和设备运行证据，不把未验证项目计入最终发布分数。

## 当前状态

- 硬件能力层：已加入 A5E/A733 通用探测，不绑定具体 A733 板卡。
- 电池诊断：已加入 BQ 通信、原始值、容量字段和校准记录入口。
- BQ 内部校准写入：明确关闭，等待官方手册命令逐项核对。
- 设备截图、空闲功耗已完成；触摸回归和首次进入页面测试仍需执行。

## 已取得的设备证据

- A5E 设备端 Qt 编译通过，无 C++ 编译警告。
- `verify/battery-0.2.0.png`：电池页能显示 4.173 V、约 711 mA、35.8 °C 和充电状态。
- BQ27220 原始寄存器读取成功：温度 `0x0c12`、电压 `0x104d`、电流 `0x02c4`、SOC `100%`。
- MeowOS 空闲 15 秒 CPU 累计占用 `0.00%`，RSS 约 `111 MB`。
- 服务状态为 `active`，日志未发现 QML 运行时错误。

当前仍未通过的项目：实际外部参考校准、BQ 内部校准命令核验、触摸手势回归和 A733 实机 profile。

运行 `scripts/quality-gate.sh` 可生成当前静态门禁结果；运行时证据完成后再更新本文件分数。
